# CS147A Final Project

For model training, model comparisons, and predictions, please refer to the main notebook:
[`model_evaluation.ipynb`](model_evaluation.ipynb)

All training setups, data loading configurations, and visualizations are handled directly in this notebook.

### Installation
You can install the necessary libraries via:
```bash
pip install -r requirements.txt
```